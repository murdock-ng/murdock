[tox]
envlist = test,style
skipsdist = True
requires = tox-envfile

[testenv]
basepython = python3
commands =
    test:       {[testenv:test]commands}
    style:      {[testenv:style]commands}

[testenv:test]
deps =
    pytest
    pytest-cov
    pytest-asyncio
    -rrequirements.txt
commands = pytest {posargs}

[testenv:style]
deps =
    black
    flake8
commands =
    flake8
    black --check --diff .
